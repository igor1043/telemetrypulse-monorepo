server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_http_version 1.1;
    proxy_buffering off;
    proxy_cache off;
    proxy_set_header Connection '';
    proxy_set_header Host $host;
    proxy_pass http://telemetry-processor:8080/api/;
  }

  location /actuator/ {
    proxy_pass http://telemetry-processor:8080/actuator/;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
